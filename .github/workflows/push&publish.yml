name: test&publish
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: npm install and npm build
        run: |
          npm i
          npm run build
      - name: npm publish
        run: |
          npm config set //registry.npmjs.org/:_authToken=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
          npm publish
        env:
          CI: true
          NODE_AUTH_TOKEN: ${{ secrets.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 }}
